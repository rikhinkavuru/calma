# control-plane service deps (the engine stays pure-stdlib; deps live here, outside it).
psycopg[binary]>=3.1   # Postgres driver (migrations + API)
# added as the API lands: fastapi, uvicorn, boto3 (R2/S3), stripe, workos
