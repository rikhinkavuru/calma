# iris-2feat — a toy classifier

Logistic regression on two Iris features (sepal length/width only — the hard, noisy subset).
Fixed seed, fixed split, nothing fancy.

**Test accuracy: 94.2%**

```
python3 eval.py
```
