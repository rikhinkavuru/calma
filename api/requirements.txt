# Deps for the Vercel Python function (api/index.py). @vercel/python installs the requirements.txt
# ADJACENT to the entrypoint, so this is the one the build uses (the repo-root copy documents the same set).
# The pure-stdlib ENGINE carries NO third-party line; these are the control-plane host's deps only.
fastapi>=0.110
pydantic>=2.6
psycopg[binary,pool]>=3.1          # Postgres driver (Supabase control-plane DB)
boto3>=1.34                   # Cloudflare R2 (S3-compatible) object storage
e2b>=1.0                      # hosted Firecracker microVM execution (engine's --isolation e2b path)
