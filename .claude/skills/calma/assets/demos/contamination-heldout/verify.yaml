{
  "_note": "Contamination demo: a benchmark accuracy reported as a zero-shot HELD-OUT result, where 10 of 25 eval questions are verbatim in the model's declared pretraining corpus. calma recomputes the accuracy (0.92, CONFIRMED), then checks the eval items against the corpus manifest: 40% are present, so the held-out claim is INVALIDATED (the number reproduces, but it is not a held-out measurement).",
  "run": {"entrypoint": "gen_fixture.py", "network": "off", "cwd": "."},
  "env": {"ecosystem": "python-stdlib", "trust": "own-code"},
  "artifacts": [
    {"path": "runs/eval.csv", "re_emit": true,
     "columns": {"prompt": {"tag": "text", "dtype": "str", "na_policy": "error"},
                 "pred": {"tag": "prediction", "dtype": "int", "na_policy": "error"},
                 "label": {"tag": "label", "dtype": "int", "na_policy": "error"}}}
  ],
  "metrics": [
    {"metric_id": "accuracy", "artifact": "runs/eval.csv",
     "binding": {"prediction": "pred", "label": "label"}, "convention": "argmax",
     "claimed_value": 0.92, "headline": true,
     "binding_status": "independently-bound", "claim_confirmed": true,
     "_claim": "author reported 92% zero-shot accuracy on the held-out benchmark"}
  ],
  "corpus": {"manifest": "runs/corpus.txt", "eval_col": "prompt"},
  "baselines": []
}
