{
  "_note": "Deflated-Sharpe demo (direct recipe path): a weak daily strategy (per-period Sharpe ~0.20) selected as the best of a 1000-config search. The producer reports a near-certain deflated Sharpe of 0.95. calma recomputes the DSR over the declared trials=1000,var_sr=0.01 search: the realized Sharpe is below the best-of-1000 no-skill benchmark (~0.33), so the recomputed DSR is ~0.02 and the 0.95 claim is REFUTED.",
  "run": {"entrypoint": "gen_fixture.py", "network": "off", "cwd": "."},
  "env": {"ecosystem": "python-stdlib", "trust": "own-code"},
  "artifacts": [
    {"path": "runs/returns.csv", "re_emit": true,
     "columns": {"strat_return": {"tag": "return", "dtype": "float", "na_policy": "error"}}}
  ],
  "metrics": [
    {"metric_id": "deflated_sharpe", "artifact": "runs/returns.csv",
     "binding": {"return": "strat_return"}, "convention": "trials=1000,var_sr=0.01",
     "claimed_value": 0.95, "headline": true,
     "binding_status": "independently-bound", "claim_confirmed": true,
     "_claim": "author reported a deflated Sharpe of 0.95 (best of 1000 backtested configs)"}
  ],
  "baselines": []
}
