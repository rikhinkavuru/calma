{
  "_note": "Realism demo (CONFIRMED-WITH-CAVEATS side): the headline Sharpe ~2.38 reproduces, but the declared frictions surface soft caveats - 3x leverage, 20% of ADV (market-impact risk), and an optimistic VWAP fill. No per-turnover cost is declared so nothing materially deflates the number; the verdict stays CONFIRMED-WITH-CAVEATS and the caveats are shown on the headline (not hidden behind a 'matches the claim' line).",
  "run": {"entrypoint": "gen_fixture.py", "network": "off", "cwd": "."},
  "env": {"ecosystem": "python-stdlib", "trust": "own-code"},
  "artifacts": [
    {"path": "runs/returns.csv", "re_emit": true,
     "columns": {"strat_return": {"tag": "return", "dtype": "float", "na_policy": "error"}}}
  ],
  "metrics": [
    {"metric_id": "sharpe", "artifact": "runs/returns.csv",
     "binding": {"return": "strat_return"}, "convention": "252",
     "claimed_value": 2.3811761839, "headline": true,
     "binding_status": "independently-bound", "claim_confirmed": true,
     "_claim": "author reported Sharpe 2.38 (run at 3x leverage, 20% of ADV, VWAP fill)"}
  ],
  "frictions": {"leverage": 3.0, "adv": 1000000, "size": 200000, "fill": "vwap"},
  "baselines": []
}
