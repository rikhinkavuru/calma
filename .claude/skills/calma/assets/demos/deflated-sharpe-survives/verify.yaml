{
  "_note": "Deflated-Sharpe demo (CONFIRMED side of the direct recipe path): a strong daily strategy (per-period Sharpe ~0.50) that was the best of 1000 configs. Its Sharpe is well above the best-of-1000 no-skill benchmark (~0.33), so it SURVIVES the multiple-testing correction: calma recomputes a Deflated Sharpe of ~0.996 and CONFIRMS the claim. The mirror of deflated-sharpe-overfit (which REFUTES a weak edge).",
  "run": {"entrypoint": "gen_fixture.py", "network": "off", "cwd": "."},
  "env": {"ecosystem": "python-stdlib", "trust": "own-code"},
  "artifacts": [
    {"path": "runs/returns.csv", "re_emit": true,
     "columns": {"strat_return": {"tag": "return", "dtype": "float", "na_policy": "error"}}}
  ],
  "metrics": [
    {"metric_id": "deflated_sharpe", "artifact": "runs/returns.csv",
     "binding": {"return": "strat_return"}, "convention": "trials=1000,var_sr=0.01",
     "claimed_value": 0.9963184649, "headline": true,
     "binding_status": "independently-bound", "claim_confirmed": true,
     "_claim": "author reported a deflated Sharpe of 0.996, survives the 1000-config search"}
  ],
  "baselines": []
}
