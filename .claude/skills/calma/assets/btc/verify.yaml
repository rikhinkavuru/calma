{
  "_note": "JSON content (valid YAML) so scripts parse it dependency-free. This is the committed contract.",
  "run": {"entrypoint": "gen_fixture.py", "network": "off", "cwd": "."},
  "env": {"ecosystem": "python-stdlib", "trust": "own-code"},
  "artifacts": [
    {"path": "runs/oos/returns.csv", "re_emit": true,
     "columns": {"strat_return": {"tag": "return", "dtype": "float", "na_policy": "error"}}},
    {"path": "runs/oos/baseline.csv", "re_emit": true,
     "columns": {"bh_return": {"tag": "return", "dtype": "float", "na_policy": "error"}}}
  ],
  "metrics": [
    {"metric_id": "total_return", "artifact": "runs/oos/returns.csv",
     "binding": {"return": "strat_return"}, "convention": "compounded",
     "claimed_value": 146.97697947938846, "headline": true,
     "binding_status": "independently-bound", "claim_confirmed": true,
     "_claim": "author reported +14,698% backtest return (in-sample, best-of-N, no costs)"}
  ],
  "baselines": [
    {"metric_id": "total_return", "artifact": "runs/oos/baseline.csv",
     "binding": {"return": "bh_return"}, "label": "buy-and-hold"}
  ]
}
