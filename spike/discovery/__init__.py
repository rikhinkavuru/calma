"""calma.spike.discovery — claim discovery (TDMR). Seed of packages/discovery.

    extract.discover(repo_dir, stdout_text="") -> [{"metric","value","location","confidence",...}, ...]
"""
from . import extract  # noqa: F401
